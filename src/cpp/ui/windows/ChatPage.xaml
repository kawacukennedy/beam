<Page
    x:Class="BlueBeam.ChatPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BlueBeam"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Background="{StaticResource BackgroundLightBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ListView x:Name="MessageList" Grid.Row="0" ItemsSource="{x:Bind Messages}" AutomationProperties.Name="Message list" TabIndex="0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Content}" Style="{StaticResource BodyStyle}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" Margin="16">
            <TextBox x:Name="MessageComposer" PlaceholderText="Type a message..." Width="300" AutomationProperties.Name="Message input" TabIndex="1" />
            <Button x:Name="SendButton" Content="Send" Background="{StaticResource PrimaryBrush}" Click="SendButton_Click" AutomationProperties.Name="Send message button" TabIndex="2" />
        </StackPanel>
    </Grid>
</Page>